class Calc {
    somar(x, y) {
        return x + y
    }

    subtrair(x, y) {
        return x - y
    }

    multiplicar(x, y) {
        return x * y
    }

    dividir(x, y) {
        return x / y
    }
}

module.exports = Calc